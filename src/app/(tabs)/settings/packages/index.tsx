import { StyleSheet, Text, View } from 'react-native';

const PackagesListScreen = () => {
  return (
    <View>
      <Text>PackagesListScreen</Text>
    </View>
  );
}

const styles = StyleSheet.create({})

export default PackagesListScreen;
